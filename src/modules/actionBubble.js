import React from 'react';

const ActionBubble = () => {
    return (

        <div className="ui actionBubble">
            <a>{ 
                // FOR LOOP THAT REPEATS AS MANY TIMES AS THERE ARE ITEMS IN THE ARRAY, L2P
            }</a>
        </div>

    );
};

export default ActionBubble;